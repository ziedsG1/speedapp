{
  "expo": {
    "name": "SpeedApp",
    "slug": "speedapp",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "speedapp",
    "userInterfaceStyle": "automatic",
    "newArchEnabled": false,
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.speedapp.runner",
      "infoPlist": {
        "NSLocationWhenInUseUsageDescription": "SpeedApp needs your location to plan running routes and display 3D maps around you.",
        "NSLocationAlwaysAndWhenInUseUsageDescription": "SpeedApp uses your location to track and plan marathon routes."
      },
      "config": {
        "googleMapsApiKey": "YOUR_GOOGLE_MAPS_API_KEY"
      }
    },
    "plugins": [
      "expo-router",
      [
        "expo-splash-screen",
        {
          "image": "./assets/images/icon.png",
          "resizeMode": "contain",
          "backgroundColor": "#0F0F1A"
        }
      ],
      [
        "expo-location",
        {
          "locationWhenInUsePermission": "SpeedApp needs your location to plan running routes and display 3D maps."
        }
      ],
      [
        "react-native-maps",
        {
          "googleMapsApiKey": "YOUR_GOOGLE_MAPS_API_KEY"
        }
      ]
    ],
    "experiments": {
      "typedRoutes": true
    },
    "extra": {
      "googleMapsApiKey": "YOUR_GOOGLE_MAPS_API_KEY",
      "serpApiKey": "YOUR_SERPAPI_API_KEY"
    }
  }
}
